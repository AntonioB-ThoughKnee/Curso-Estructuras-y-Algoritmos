// Nombre: Listar en preorden
// Paramentros: A (tipo Arbol) variable global
// Efecto: Listar (Imprimir) las etiquetas del árbol haciendo un recorrido
// en preOrden
// Requiere: Arbol inicializado pasado como variable global
// Modifica: El algoritmo no modifica al arbol

global A

ListarPreOrden(){
    if !A.Vacío(){
    ListarPreOrdenR(A.Raíz())
    }
}
ListarPreOrdenR(n){
    Imprimir(A.Etiq(n))
    n1= A.HMI(n)
    while n1 != nodonulo{
    PreOrdenR(n1,A)
    n1 = A.HD(n1)
    }
}